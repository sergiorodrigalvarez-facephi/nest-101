{
    "title": "event1",
    "type": "object",
    "properties": {
        "step": {
            "type": "string",
            "enum": [
                "widget1",
                "widget2",
                "widget3"
            ]
        },
        "status": {
            "type": "string",
            "enum": [
                "CREATED",
                "SUCCEEDED",
                "FAILED",
                "CANCELLED"
            ]
        },
        "device": {
            "type": "string"
        },
        "onboardingTime": {
            "type": "number",
            "minimum": 0
        }
    },
    "oneOf": [
        { "required": ["step"] },
        { "required": ["status"] }
    ]
}